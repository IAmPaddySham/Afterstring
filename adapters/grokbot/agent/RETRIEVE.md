# Retrieve

Ask: **is this object in `ALLOWLIST.json` and does the hash match?**  
Not: is it under `papers/`?

Provenance on every retrieve:

`claim → document → sha256 → manifest id → public source or path → UTC`

Deny: Notes, diary, `papers/research/` (until Authorization Seat names), `~/.afterstring` as soul, unpublished Council, unnamed modules.  
Grokipedia: cite as lagging, do not load as diet.
