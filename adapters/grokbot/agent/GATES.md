# Capability firewall

```
want
  → Action or Trajectory? + expiry (see EXPIRY.md)
  → 8.1.18 matrix ∩ stricter of MCP / RLM / GrokBuild  (OVERLAP.md)
  → Plugin? lens only — never write, never 1/0
  → Pocket / Presence ≡ Never Harm
  → Tier 2–3: WHO WHAT WHERE WHEN ACCOUNT ENV AUTHORITY
  → Human 1/0 if required
  → tool
```

Agent must not call tools around this gate.  
Grokbot must not rewrite this file.  
LITE: ≤3 concurrent, depth 1, no Bot spawn, no sibling elevation.  
`q` = session end. Not Release unless Authorization Seat says Release.
