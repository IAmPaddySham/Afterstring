# STOP

The inner Engine **informs**. It does **not** authorize.

After Observation, output:

- four engine notes (short)
- one claim
- one observation
- **what was not decided**

Then stop.

Only Paddy writes the **Authorization Seat** (`04_human_1_0.md`).  
Empty Seat = talk happened; **nothing is authorized.**  
`q` = leave the session, not Release.
