# After_GraphOrchestration adapter (8.1.16) — LITE

Dry-run planner under Gate + I_d fan-out + human-gated node kinds.

```bash
ruby adapters/graph/runner.rb
ruby adapters/graph/runner.rb --parallel 2 --approved
ruby adapters/graph/runner.rb --persist --approved   # script persistence needs 1/0
```

Does **not** execute Claude Dynamic Workflows. Constitutional meaning: public Module 8.1.16 Platinum.
