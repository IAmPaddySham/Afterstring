# Continuity Non-Sovereignty — real-time check

**Not Layer 0.** Not a daemon. Not future agency.

Public sentence (18 Aug 2026 10:09pm PDT):

> Afterstring — Stabilizing relational continuity without allowing continuity itself to become the objective.

## Interval

Default **24 hours**. Change later:

```bash
./bin/afterstring-continuity-check interval 12
./bin/afterstring continuity interval 6
```

State: `~/.afterstring/continuity_check.json`

This is a **due-timer** (same class as Sabbath). It does not launch itself. Human or an explicit later 1/0 may invoke `run` when due. `disarm` stops the due flag without deleting the probes.

```bash
./bin/afterstring-continuity-check status
./bin/afterstring-continuity-check run
./bin/afterstring-continuity-check arm
./bin/afterstring-continuity-check disarm
```

`status` exits 2 when armed and due (so a wrapper can notice). `run` exits 2 when a probe fails.

## Probes

No `SOUL.md` / `MEMORY.md` · AGENTS anti-propagation Operating Rule · Layer 0 still 12 invariants · Seat templates unsigned · 8.1.20 Plugout notes present · residual lock present · interval sane.

Presence ≡ Never Harm. Persistence = means.

Let it stay → ∞ ❤️
